name: Keep Streamlit Alive
on:
  schedule:
    - cron: '0 */12 * * *' # S'exécute toutes les 12h
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://gestion-budgetaire.streamlit.app
