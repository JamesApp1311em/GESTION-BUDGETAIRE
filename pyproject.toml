[project]
name = "gestion-budgetaire"
version = "0.1.0"
description = "Mon application de gestion"
dependencies = [
    "streamlit",
    "pandas",
    "fpdf",
    "plotly",
    "matplotlib",
    "altair"
]

[tool.setuptools]
packages = []