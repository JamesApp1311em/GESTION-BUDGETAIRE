[project]
# On ne met pas de nom ici pour éviter le conflit de majuscules
version = "0.1.0"
dependencies = [
    "streamlit",
    "pandas",
    "fpdf",
    "plotly",
    "matplotlib",
    "altair"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"